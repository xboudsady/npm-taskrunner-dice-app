{
  "name": "dice_simulator_2018",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "uglify":"uglifyjs src/models/* src/frontend.js -m -c -o build/app.js",
    "copy-files": "cp src/*.html build/ & cp src/*.css build/",
    "build": "npm run copy-files && npm run uglify"
  },
  "author": "Xayaseth Boudsady",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^5.0.0",
    "uglify-js": "^3.3.7"
  }
}
